# OperonAI Production Environment
# These values override .env during production builds

# In production, use relative paths since the frontend and API are on the same domain
VITE_API_URL=/api
VITE_WEBHOOK_URL=/api/webhooks
